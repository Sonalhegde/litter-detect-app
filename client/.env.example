# Inference API origin. In development, the dev server proxies /inference-api → http://127.0.0.1:8000.
# Override only if the backend runs on a different host or port.
VITE_INFERENCE_API_URL=http://127.0.0.1:8000
