# URL of the inference API backend.
# In development, the Vite proxy rewrites /inference-api → http://127.0.0.1:8000,
# so you only need this for a separate deployment (e.g. pointing at Render).
VITE_INFERENCE_API_URL=https://litter-detect-inference.onrender.com
