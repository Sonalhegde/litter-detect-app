fastapi==0.141.1
uvicorn[standard]==0.30.6
numpy==1.26.0
onnxruntime==1.18.1
opencv-python-headless==4.10.0.84
pillow==12.3.0
python-multipart==0.0.32
# Scene-relevance checker (CLIP zero-shot) — optional.
# If these are not installed, the scene checker falls back to pass-through
# and all images proceed to detection without a relevance check.
# Uncomment to enable on a deployment with sufficient memory (>= 2 GB RAM).
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch==2.3.1+cpu
# open_clip_torch==2.24.0
