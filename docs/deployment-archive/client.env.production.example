# Production frontend (Vercel) — copy to client/.env before `pnpm run build:frontend`
# Replace with your actual Render inference service origin (no trailing slash).
VITE_INFERENCE_API_URL=https://litter-detect-inference.onrender.com
